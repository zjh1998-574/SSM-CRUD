<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!--springmvc的配置文件： 声明controller， 视图解析器等web开发中的的对象-->
    <context:component-scan base-package="com.cqupt.crud.controller"></context:component-scan>


    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!--
  作用：
   1. 创建HttpMessageConverter接口的7个实现类对象， 处理java对象到json的转换
   2. 解决静态资源中，动态资源访问失败的问题。
-->

    <!-- 将springmvc不能处理的请求静态资源交给tomcat -->
    <mvc:default-servlet-handler/>
    <!--
    作用：
    1. 创建HttpMessageConverter接口的7个实现类对象， 处理java对象到json的转换
    2. 解决静态资源中，动态资源访问失败的问题。
    -->
    <mvc:annotation-driven></mvc:annotation-driven>
</beans>